[package]
name = "lau-room-native"
version = "0.1.0"
edition = "2021"
description = "Room-native agent system — the room IS the agent's context"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
